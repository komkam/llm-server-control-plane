# Machine-local runtime configuration. Do not commit the copied runtime.env.
# All bundled listeners default to loopback; expose them through your own
# reverse proxy or Tailscale Serve configuration.
DASHBOARD_HOST=127.0.0.1
DASHBOARD_PORT=7000
DASHBOARD_HEALTH_URL=http://127.0.0.1:7000/api/health
